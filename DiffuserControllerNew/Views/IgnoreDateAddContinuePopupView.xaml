<UserControl
    x:Class="DiffuserControllerNew.Views.IgnoreDateAddContinuePopupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DiffuserControllerNew.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:Background="White"
    mc:Ignorable="d">
    <Border
        Width="Auto"
        Background="White"
        BorderBrush="#404040"
        BorderThickness="1"
        CornerRadius="7">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="31" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border
                Grid.Row="0"
                Background="#F3F3F3"
                BorderBrush="#404040"
                BorderThickness="0,0,0,1"
                CornerRadius="7 7 0 0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="70" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        Padding="10"
                        VerticalAlignment="Center"
                        Text="연속일의 마지막 일자 설정" />
                    <ui:Button
                        Grid.Column="1"
                        Command="{Binding PopupHeaderCloseCommand}"
                        Content="X"
                        Style="{StaticResource BtnPopupHeaderClose}" />
                </Grid>
            </Border>
            <Border Grid.Row="1" Padding="20">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Style="{StaticResource LeftSubTitle}"
                        Text="시작일" />
                    <TextBox
                        Grid.Row="0"
                        Grid.Column="2"
                        Style="{StaticResource SelectedContent}"
                        Text="{Binding StartDateAt}" />
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="0"
                        Style="{StaticResource LeftSubTitle}"
                        Text="종료일" />
                    <Border
                        Grid.Row="2"
                        Grid.Column="2"
                        BorderBrush="#5A6178"
                        BorderThickness="1">
                        <local:CalendarEx SelectedDate="{Binding SelectedEndDt, Mode=TwoWay}" />
                    </Border>

                    <TextBlock
                        Grid.Row="4"
                        Grid.Column="0"
                        Style="{StaticResource LeftSubTitle}"
                        Text="범위" />
                    <TextBox
                        Grid.Row="4"
                        Grid.Column="2"
                        Style="{StaticResource SelectedContent}"
                        Text="{Binding RangeVal}" />

                    <ui:Button
                        Grid.Row="6"
                        Grid.Column="2"
                        Width="80"
                        Height="30"
                        HorizontalAlignment="Left"
                        Command="{Binding PopupApplyCommand}"
                        Content="적용"
                        Style="{StaticResource BtnFunctionBlue}" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
